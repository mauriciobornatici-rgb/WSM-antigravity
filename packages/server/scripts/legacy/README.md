# Legacy Scripts

This folder contains one-off migration and debug scripts kept for reference.

- They are **not** part of the runtime server startup.
- They are **not** invoked by npm scripts.
- Prefer creating new migrations under `packages/server/migrations/` for schema changes.

